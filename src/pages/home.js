import getData from '../utils/getData';

const Home = async () => {
  // const products = await getData();
  const view = `
    <div class="products">
      <h1>Contenido del Home</h1>
    </div>
  `;
  return view;
};

export default Home;